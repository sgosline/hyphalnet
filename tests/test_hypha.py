import pytest
import igraph as ig

from hyphalnet.hypha import hyphalNetwork


def tests_create_mini_hyp():
    newg = ig.Graph()
    new_weights = {}
    #new_hyp = hyphalNetwork(new_weights, newg)
    ##assert something
    assert(True)
